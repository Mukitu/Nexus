// Firebase configuration placeholder
// Firebase will be set up later - currently using mock data

export const auth = null;
export const db = null;
export const storage = null;
export const googleProvider = null;

export default null;
